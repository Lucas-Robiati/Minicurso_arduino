/*

Vamos usar a técnica PWM(Pulse Width Modulation)
    
Modulamos por uma largura de pulso (sinal) e que pode ser utilizado 
para emular um sinal analógico através de pulsos digitais

Somente os pinos com "~" pode realizar o PWD 

*/

//Representar os pinos com nomes logicos
#define led 6
#define pot A5

void setup() 
{
  //Setando a Porta digital do arduino como saida
  pinMode(led, OUTPUT);

  /*
  O potenciometro não tem necessidade de declarar a 
  porta como entrada pois elas ja são definidas por 
  padrão como entrada.
  Se quiser visualizar é possivel utilizar 
  o debuger do Wokwi
  */
  
  //Inicia comunicação serial
  Serial.begin(9600);

}

void loop() 
{
  
  int potValue = analogRead(pot);
  int ledEnergi = map(potValue, 0, 1023, 0, 255);
  Serial.println(ledEnergi);
  analogWrite(led, ledEnergi);
  delay(500);
}


